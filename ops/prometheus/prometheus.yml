global:
  scrape_interval: 15s

rule_files:
  - rules.yml

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["prometheus:9090"]

  - job_name: "toyprod"
    metrics_path: /metrics
    static_configs:
      - targets: ["toyprod:8000"]